function lengthOfLIS(nums) {
    let arryNuevo = new Array(nums.length).fill(1)
    for (let i = 0; i < nums.length; i++) {
        const element = nums[i];
        
    }
}

module.exports = lengthOfLIS;
